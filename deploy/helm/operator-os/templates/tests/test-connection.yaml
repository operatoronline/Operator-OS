apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "operator-os.fullname" . }}-test-connection"
  labels:
    {{- include "operator-os.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "operator-os.fullname" . }}:{{ .Values.service.port }}/health']
  restartPolicy: Never
