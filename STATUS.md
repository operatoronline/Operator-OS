# Operator OS — Production Readiness Status

**Last Updated:** 2026-03-08
**Current Phase:** 6 — Hardening & Launch
**Overall Progress:** 86%

---

## Phase Overview

| # | Phase | Status | Target | Progress |
|---|---|---|---|---|
| 1 | Foundation (SQLite, logging, metrics, encryption) | ✅ Done | Weeks 1–4 | 100% |
| 2 | User Management (accounts, tenancy, auth) | ✅ Done | Weeks 5–8 | 100% |
| 3 | Billing & Plans (Stripe, metering) | ✅ Done | Weeks 9–12 | 100% |
| 4 | Service Integrations (OAuth, vault, marketplace) | ✅ Done | Weeks 13–16 | 100% |
| 5 | Scaling & Reliability (PostgreSQL, NATS, K8s) | ✅ Done | Weeks 17–20 | 100% |
| 6 | Hardening & Launch (security, compliance, testing) | 🔄 In Progress | Weeks 21–24 | 17% |

---

## Phase 1: Foundation

### Tasks

| ID | Task | Priority | Status | Assignee | Notes |
|---|---|---|---|---|---|
| F1 | Replace JSON sessions with SQLite | P0 | ✅ Done | Cosmo | Implemented `SessionStore` interface + `SQLiteStore` backend. `SessionManager` delegates to store when present via `NewSessionManagerWithStore()`. 15 tests pass. WAL mode, write-through. |
| F2 | Replace JSON state manager with SQLite | P0 | ✅ Done | Cosmo | Implemented `StateStore` interface + `SQLiteStateStore` backend. `Manager` delegates to store via `NewManagerWithStore()`. 9 new tests pass. WAL mode, write-through. Existing JSON tests unaffected. |
| F3 | Replace auth store with encrypted SQLite | P0 | ✅ Done | Cosmo | Implemented `CredentialStore` interface + `SQLiteCredentialStore` backend. AES-256-GCM encryption with Argon2id key derivation from `OPERATOR_ENCRYPTION_KEY`. Base64 fallback when no key set (with warning). Package-level functions delegate via `SetGlobalCredentialStore()`. 22 new tests pass. |
| F4 | Add structured logging (zerolog) | P0 | ✅ Done | Cosmo | Replaced `pkg/logger` internals with `rs/zerolog`. All 20 existing API functions preserved (Debug/Info/Warn/Error/Fatal × plain/C/F/CF). Added 12 context-aware functions (`*Ctx`) with correlation ID propagation via `WithCorrelationID(ctx, id)`. JSON output via `OPERATOR_LOG_FORMAT=json`, console (default). Level via `OPERATOR_LOG_LEVEL` env var. File logging via multi-writer. 10 new test cases (correlation ID, structured JSON, context functions, env config, file logging). |
| F5 | Add OpenTelemetry metrics | P1 | ✅ Done | Cosmo | Prometheus endpoint at `/metrics` via `prometheus/client_golang`. New `pkg/metrics` package with 11 collectors: LLM request duration/tokens/errors, sessions active/messages, bus messages/queue depth, tool execution duration/count, uptime, info. Convenience helpers (`RecordLLMRequest`, `RecordToolExecution`, `RecordBusMessage`). Instrumented `tools.ToolRegistry.ExecuteWithContext` and `bus.MessageBus.Publish*`. Registered on health server mux. `metrics.Init()` called at gateway startup. 11 tests pass. |
| F6 | Add session TTL and eviction | P1 | ✅ Done | Cosmo | `EvictableStore` interface extends `SessionStore` with `SessionCount`, `DeleteSession`, `EvictExpired`, `EvictLRU`. `SQLiteStore` implements all four. `Evictor` runs periodic background sweeps (TTL then LRU). `DefaultEvictorConfig()`: 24h TTL, 10K max sessions, 5min interval. 14 new tests pass. |
| F7 | Add automated SQLite backup | P1 | ✅ Done | Cosmo | New `pkg/backup` package. `VacuumInto()` for atomic snapshots. `Scheduler` with configurable interval, retention (MaxBackups), and auto-pruning. `ListBackups()` utility. 14 tests pass. |
| F8 | Database migration framework | P1 | ✅ Done | Cosmo | New `pkg/dbmigrate` package. Embedded SQL migrations with version tracking in `schema_migrations` table. `Migrator` loads `.sql` files from `embed.FS`, runs pending migrations in version-ordered transactions, skips already-applied. `AutoMigrate(db)` convenience for startup. 3 built-in migrations (sessions, state, credentials). `NewFromList()` for programmatic use. 17 tests pass. |

### Definition of Done — Phase 1
- [x] All session data persists in SQLite (not JSON files)
- [x] All state data persists in SQLite
- [x] Credentials encrypted at rest
- [x] Structured JSON logging with correlation IDs
- [x] Prometheus metrics endpoint functional
- [x] Session eviction prevents unbounded memory growth
- [x] Automated backup runs on schedule
- [x] Database migration framework with version tracking
- [x] All existing tests pass
- [x] New tests cover SQLite stores (≥80% coverage for new code)
- [x] `make test` passes clean

---

## Phase 2: User Management

### Tasks

| ID | Task | Priority | Status | Notes |
|---|---|---|---|---|
| U1 | Users table + registration API | P0 | ✅ Done | New `pkg/users` package. `UserStore` interface + `SQLiteUserStore` backend. `POST /api/v1/auth/register` with email validation, bcrypt hashing (cost 12), case-insensitive email. Migration `004_create_users.sql`. 28 new tests (18 store + 10 API). |
| U2 | Login + JWT issuance | P0 | ✅ Done | `POST /api/v1/auth/login` + `POST /api/v1/auth/refresh`. `TokenService` with HMAC-SHA256 (golang-jwt/jwt/v5). Access tokens (15min) + refresh tokens (7d). `AuthMiddleware` for protected routes. Context helpers (`UserIDFromContext`, `EmailFromContext`, `ClaimsFromContext`). Account status checks (suspended/deleted). Anti-enumeration error messages. 28 new tests (JWT: 18, Login API: 10). |
| U3 | Email verification flow | P1 | ✅ Done | New `VerificationStore` interface + `SQLiteVerificationStore` backend. Crypto-random hex tokens (32 bytes) with configurable TTL (default 24h). `VerifyEmail()` validates token existence/expiry/usage, marks user verified, transitions status pending→active. `ResendVerification()` with cooldown enforcement (default 60s). API: `POST /api/v1/auth/verify-email` (token validation with distinct error codes: not_found/expired/used/already_verified), `POST /api/v1/auth/resend-verification` (anti-enumeration: returns 200 for nonexistent emails). `DeleteExpired()` for token cleanup. Migration `007_create_verification_tokens.sql`. `NewAPIFull()` constructor. 32 new tests (store: 10, function: 6, API: 14, persistence: 1, flow: 1). |
| U4 | Tenant-scoped sessions | P0 | ✅ Done | New `TenantStore` implements `SessionStore` with full tenant isolation. Scoped key format `tenant:<id>:<key>`. `TenantStoreFactory` creates stores per-tenant from shared DB. Context helpers `WithTenantID`/`TenantIDFromContext`. Migration `005_add_tenant_id.sql` adds `tenant_id` column + indexes. `EvictableStore` methods (SessionCount, Delete, EvictExpired, EvictLRU) all tenant-scoped. `ListSessions()` returns unscoped keys. 25 new tests. |
| U5 | Per-user agent configuration | P0 | ✅ Done | New `pkg/agents` package. `UserAgentStore` interface + `SQLiteUserAgentStore` backend. Full CRUD API: `GET/POST /api/v1/agents`, `GET/PUT/DELETE /api/v1/agents/{id}`, `POST /api/v1/agents/{id}/default`. Per-user isolation enforced at API layer. Agents have: name, description, system_prompt (persona), model, model_fallbacks, tools, skills, max_tokens, temperature, max_iterations, is_default, status. Unique name per user. Max 50 agents/user. Migration `006_create_user_agents.sql`. 40 new tests (22 store + 18 API). |
| U6 | Per-user rate limiting | P1 | ✅ Done | New `pkg/ratelimit` package. Token bucket algorithm with configurable plan tiers (free/starter/pro/enterprise). `Limiter` with in-memory fast path + `SQLiteRateLimitStore` persistence. HTTP `Middleware` adds rate limit headers (X-RateLimit-Limit/Remaining/Daily-*) and returns 429 with Retry-After. `StatusHandler` for `GET /api/v1/rate-limit/status`. `PersistMiddleware` for periodic state saving. Per-minute burst + daily rolling limits. Plan upgrade resets bucket. Migration `008_create_rate_limits.sql`. 34 new tests. |
| U7 | Audit logging | P1 | ✅ Done | New `pkg/audit` package. `AuditStore` interface + `SQLiteAuditStore` backend. Event model with builder pattern (`NewEvent`, `WithUser`, `WithActor`, `WithResource`, `WithDetail`, `WithFailure`, `WithIPAddress`, `WithUserAgent`). Action constants for auth (login/register/verify/password), agent CRUD, tool execution, config changes, admin actions. `QueryFilter` with user_id, action (exact + prefix matching), resource, status, time range, pagination. `Logger` wrapper with configurable error handler. HTTP `Middleware` captures response status, IP (X-Forwarded-For/X-Real-IP), user agent. API: `GET /api/v1/audit/events` (list with filters), `GET /api/v1/audit/events/count`. `DeleteBefore()` for retention cleanup. Migration `009_create_audit_log.sql` with indexes on user_id, action, timestamp, resource. 36 new tests. |
| U8 | Admin API | P1 | ✅ Done | New `pkg/admin` package. `AdminMiddleware` checks `role` field on user (role-based access control). Admin REST API: `GET/PUT/DELETE /api/v1/admin/users/{id}`, `POST .../suspend`, `POST .../activate`, `POST .../role`, `GET /api/v1/admin/stats`, `GET /api/v1/admin/audit`. User role field (`user`/`admin`) with migration `010_add_user_role.sql`. Self-protection: admins cannot demote, suspend, or delete themselves. Platform stats: total/active/pending/suspended user counts. Audit log query passthrough with filters. 36 new tests. |

---

## Phase 3: Billing & Plans

### Tasks

| ID | Task | Priority | Status | Notes |
|---|---|---|---|---|
| B1 | Plan definitions (config-driven) | P0 | ✅ Done | New `pkg/billing` package. `Plan` model with ID, name, pricing (cents), `PlanLimits` (agents, messages/mo, tokens/mo, integrations, storage, team members, allowed models, custom skills, API access, rate limits). Four tiers: Free ($0), Starter ($9/mo), Pro ($29/mo), Enterprise (custom). `Catalogue` with concurrent-safe Get/List/ListActive/Replace/Merge. `SubscriptionStore` interface + `SQLiteSubscriptionStore` backend. `Subscription` model with status lifecycle (active/trialing/past_due/canceled/expired/paused), Stripe fields, period tracking. REST API: `GET /api/v1/billing/plans`, `GET /api/v1/billing/plans/{id}`. Limit helpers `CheckLimit`/`IsUnlimited`. JSON serialization for config loading. Migration `011_create_subscriptions.sql`. 50 new tests. |
| B2 | Stripe integration | P0 | ✅ Done | `StripeClient` (HTTP-based, no SDK). `WebhookHandler` with HMAC-SHA256 signature verification. `StripeAPI` endpoints: `POST /checkout`, `POST /portal`, `GET /subscription`, `POST /webhook`. Plan↔Price mapping via `PlanPriceMap`. Webhook event routing: checkout.session.completed, customer.subscription.updated/deleted, invoice.payment_failed/succeeded. 60 new tests. |
| B3 | Token usage metering | P0 | ✅ Done | New `UsageStore` interface + `SQLiteUsageStore` backend. `UsageEvent` model with user, model, provider, input/output/total tokens, session_key, agent_id, duration_ms, estimated_cost. Auto-computes total_tokens from input+output when not explicitly set. `GetSummary()` for aggregate usage over time range. `GetByModel()` for per-model breakdown (sorted by total tokens DESC). `GetDaily()` for daily aggregation using SUBSTR for SQLite compatibility. `GetCurrentPeriodUsage()` and `GetCurrentPeriodMessages()` for billing period enforcement. `ListEvents()` with filters (user, model, time range, pagination). `DeleteBefore()` for retention cleanup. `UsageSummary`, `ModelUsage`, `DailyUsage` response models. `UsageQuery` filter struct. Integration with `CheckLimit()` for plan enforcement. Migration `012_create_usage_events.sql` with indexes on user_id, (user_id, created_at), (user_id, model), created_at. 30 new tests. |
| B4 | Usage dashboard API | P1 | ✅ Done | New `UsageAPI` struct with 5 REST endpoints: `GET /api/v1/billing/usage` (current period summary with subscription period awareness), `GET /api/v1/billing/usage/daily` (per-day breakdown with `days` shorthand param), `GET /api/v1/billing/usage/models` (per-model breakdown), `GET /api/v1/billing/usage/events` (paginated raw events with model/time filters), `GET /api/v1/billing/usage/limits` (current usage vs plan limits with token/message counters). Subscription-aware period detection (falls back to calendar month for free users). `parseTimeRange` with `since`/`until` RFC3339 params + `days` shorthand. `beginningOfMonth` helper. All endpoints require auth, return 503 when usage store not configured. Multi-user isolation enforced. 27 new tests. |
| B5 | Overage handling | P1 | ✅ Done | `OverageEnforcer` with 4-level threshold system (warning→soft_cap→hard_cap→blocked). `OverageThresholds` (configurable, default 80/90/100/120%). `ThrottleConfig` with fallback model, delay, rate reduction. `CheckTokens`/`CheckMessages`/`CheckAll`/`CheckUser`/`GetFullStatus` methods. `OverageMiddleware` adds X-Overage-* headers, throttles at hard cap (artificial delay), blocks at 120%. `OverageAPI` with `GET /api/v1/billing/overage` endpoint. Multi-user isolation. 36 new tests. |
| B6 | Plan upgrade/downgrade | P1 | ✅ Done | New `pkg/billing/plan_change.go`. `PlanChangeService` with `ChangePlan()` and `Preview()`. `PlanChangeDirection` (upgrade/downgrade/same) via `ComparePlans()` with tier ordering. `PlanChangeMode` (immediate/at_period_end) with `DefaultChangeMode()` (upgrades=immediate, downgrades=at_period_end). `PlanChangeRequest`/`PlanChangeResult` models. Handles 3 scenarios: free→paid (creates subscription), paid→paid (updates with proration), paid→free (cancels). `estimateProration()` calculates prorated charge/credit based on remaining period fraction. Stripe integration: `stripeUpdateSubscription()` with item-level price swap and configurable proration_behavior. `OnPlanChanged` callback for audit/notifications. `PlanChangeAPI` with `POST /api/v1/billing/change-plan` (error mapping: 409 already_on_plan, 400 invalid_plan, 502 stripe_error) and `POST /api/v1/billing/preview-change`. Self-hosted mode: downgrades apply immediately without Stripe. 42 new tests. |

---

## Phase 4: Service Integrations

### Tasks

| ID | Task | Priority | Status | Notes |
|---|---|---|---|---|
| S1 | OAuth 2.0 framework (PKCE) | P0 | ✅ Done | New `pkg/oauth` package. Generic server-side OAuth 2.0 with PKCE. `ProviderRegistry` for declarative provider config. `StateStore` interface + `SQLiteStateStore` for CSRF state persistence. `Service` with `StartFlow`/`HandleCallback`/`RefreshToken`. `API` with 4 endpoints: `GET /providers`, `POST /authorize`, `GET /callback`, `POST /refresh`. Replay prevention, state expiry, scope merging, extra auth params. Migration `013_create_oauth_states.sql`. 40 new tests. |
| S2 | Encrypted credential vault | P0 | ✅ Done | Per-user per-integration encrypted token storage. `VaultStore` interface + `SQLiteVaultStore` backend. AES-256-GCM with Argon2id key derivation. Base64 fallback without key. `VaultCredential` model (access/refresh/ID tokens, scopes, label, status, expiry). Full CRUD: Store (upsert), Get (user+provider), GetByID, ListByUser (with status filter), Delete, DeleteByID, Revoke, DeleteExpired. `StoreFromTokenResponse` convenience. Multi-user isolation. Migration `014_create_credential_vault.sql`. 45 new tests. |
| S3 | Integration registry (declarative manifests) | P0 | ✅ Done | New `pkg/integrations` package. `Integration` manifest model with ID, name, category, description, auth_type (oauth2/api_key/none), `OAuthConfig` (with dynamic params, PKCE, extra auth params), `APIKeyConfig`, `ToolManifest` array, required_plan, status (active/beta/deprecated). `IntegrationRegistry` (concurrent-safe) with Register/Replace/Get/List/ListByCategory/ListActive/Remove/Count/Categories. `LoadFromJSON`/`LoadMultipleFromJSON` for declarative manifest loading. `GetToolManifest`/`AllToolNames` for cross-integration tool lookup. `UserIntegrationStore` interface + `SQLiteUserIntegrationStore` backend for per-user integration state tracking. `UserIntegration` model with status lifecycle (pending/active/failed/revoked/disabled), config map, scopes, error_message, last_used_at. Full CRUD: Create (unique constraint on user+integration), Get, GetByID, ListByUser (with status filter), Update, UpdateStatus, Delete, RecordUsage, CountByUser. `IntegrationTool` adapter implements `tools.Tool` interface, delegates execution to `IntegrationToolExecutor` function. `ToolRegistrar` bridges integration manifests to `tools.ToolRegistry` with `RegisterAll`/`RegisterIntegration`. `NewHTTPToolExecutor` for REST-based integrations with token/endpoint/baseURL resolvers. `API` with 5 REST endpoints: `GET /api/v1/integrations` (list active, by category, sanitized—no secrets), `GET /api/v1/integrations/categories`, `GET /api/v1/integrations/{id}`, `GET/POST /api/v1/user/integrations` (list/create with auth), `GET/DELETE /api/v1/user/integrations/{id}`. Migration `015_create_user_integrations.sql` with indexes on user_id, (user_id, status), integration_id, UNIQUE(user_id, integration_id). 80 new tests. |
| S4 | Token refresh manager | P0 | ✅ Done | `TokenRefreshManager` with background sweep loop, per-credential singleflight mutex (concurrent refresh prevention), exponential backoff retries (configurable max retries + delay), `EnsureFresh()`/`RefreshCredential()` for on-demand refresh. `SweepableVault` interface + `SQLiteVaultStore.ListExpiring()` for efficient expiry queries. `RefreshStatus` for retry state introspection, `ResetRetries()` for manual recovery. `OnRefresh`/`OnError` callbacks. Re-reads vault after acquiring lock to avoid redundant refreshes. 30 new tests. |
| S5 | First integrations: Google (Gmail, Drive, Calendar) | P1 | ✅ Done | New `pkg/integrations/google` package. Three integration manifests: Gmail (7 tools: list/get/send/search messages, list/modify labels, trash), Google Drive (7 tools: list/get/search/download files, create file/folder, delete), Google Calendar (7 tools: list/get/create/update/delete events, list calendars, quick add). All 21 tools have full JSON Schema parameters, required scopes, and rate limits. `NewOAuthProvider()` for Google OAuth 2.0 with PKCE, offline access, consent prompt. `RegisterAll()` for batch registration into `IntegrationRegistry`. `ResolveEndpoint()` maps tool names to Google API HTTP method + path with parameter substitution and URL escaping. `ResolveBaseURL()` per-integration API base URLs (Gmail/Drive/Calendar). `NewGoogleHTTPExecutor()` wires into generic HTTP tool executor. 35 new tests. |
| S6 | Shopify integration | P1 | ✅ Done | OAuth + Admin API tools (Products, Orders, Customers) |
| S7 | Integration management API | P1 | ✅ Done | New `ManagementAPI` in `pkg/integrations/management_api.go` orchestrating registry, user integration store, OAuth service, credential vault, and token refresh manager. Full integration lifecycle: `POST /connect` (OAuth 2.0 flow initiation, API key storage, no-auth connections; handles reconnecting failed/revoked integrations), `POST /disconnect` (revokes vault tokens, resets refresh retries, deletes records), `GET /status` (lists all user integrations with enriched token health from vault + refresh retry state), `GET /{id}/status` (single integration detail), `POST /{id}/enable` (re-enable disabled), `POST /{id}/disable` (preserve tokens while pausing), `POST /{id}/reconnect` (re-initiate OAuth for failed/revoked), `PUT /{id}/config` (merge config updates). `IntegrationStatus` model with `TokenStatus` (has_access_token, has_refresh_token, expires_at, is_expired, needs_refresh) and `RefreshStatus` enrichment. `ConnectRequest`/`ConnectResponse` models. Multi-user isolation enforced. 45 new tests. |
| S8 | Per-agent scope narrowing | P1 | ✅ Done | `AgentIntegrationScope` model + `AgentScopeFilter` in `pkg/integrations/scope_filter.go`. Per-agent integration restriction via `AllowedIntegrations` field on `UserAgent`. Supports open access (nil/empty = all integrations), integration-level restriction, tool-level restriction, and scope-level restriction. `NewAgentScopeFilter()` creates filter from config. `IsIntegrationAllowed`/`IsToolAllowed`/`IsScopeAllowed` for checking. `FilterTools`/`FilterToolNames` for bulk filtering. `EffectiveScopes` computes intersection of allowed and default OAuth scopes. `ValidateAgentScopes` validates against registry (integration existence, tool names, scope names). `ScopeSummary` for human-readable display. Agent model updated with `AllowedIntegrations []AgentIntegrationScope` field. SQLite store updated: `allowed_integrations` column (JSON), marshal/unmarshal helpers. Create/Update/GetByID/ListByUser/GetDefault all persist the field. API updated: `CreateAgentRequest`/`UpdateAgentRequest`/`AgentResponse` include `allowed_integrations`. Partial update support (pointer-based). Validation on create/update (integration_id required). Migration `016_add_agent_integrations.sql`. 47 new tests (37 scope filter + 10 store/persistence). |

---

## Phase 5: Scaling & Reliability

### Tasks

| ID | Task | Priority | Status | Notes |
|---|---|---|---|---|
| R1 | PostgreSQL session/state store (SaaS mode) | P0 | ✅ Done | New `pkg/pgstore` package with connection pooling (pgx/v5 stdlib driver). `PGStore` in `pkg/session/pg_store.go` implements `SessionStore` + `EvictableStore` (full parity with SQLiteStore). `PGStateStore` in `pkg/state/pg_store.go` implements `StateStore`. `PGCredentialStore` in `pkg/auth/pg_credential_store.go` implements `CredentialStore` with AES-256-GCM encryption. All use PostgreSQL-native types (TIMESTAMPTZ, BYTEA, BIGSERIAL) and $N placeholders. Shared `*sql.DB` ownership model (caller manages lifecycle). Connection pool defaults: 25 max open, 10 max idle, 5m lifetime, 1m idle time. All stores accept `*sql.DB` (no DSN — Open() in pgstore creates the pool). Integration tests skip without `OPERATOR_TEST_PG_DSN`. 40 new tests (20 session, 11 state, 9 credential + 3 pgstore). |
| R2 | NATS JetStream message bus | P0 | ✅ Done | `Bus` interface + `NATSBus` implementation with JetStream at-least-once delivery |
| R3 | Stateless worker architecture | ✅ Done | New `pkg/worker` package with stateless, horizontally-scalable message processing |
| R4 | Kubernetes Helm chart | P1 | ✅ Done | Full Helm chart in `deploy/helm/operator-os/`. See notes below. |
| R5 | Redis session cache | P1 | ✅ Done | New `pkg/rediscache` package. `RedisClient` minimal interface (Get/Set/Del/Ping/DBSize) satisfied by `*redis.Client` and `*redis.ClusterClient`. `RedisCache` wraps any `SessionStore` as read-through/write-through cache: reads check Redis first (cache hit), fall through to backing store on miss and populate cache; writes go to backing store then invalidate cache. `cachedSession`/`cachedMessage` compact JSON serialization with short field names. `Config` with `KeyPrefix` (default "sess:"), `TTL` (default 30min), optional `Ctx`. `New(client, backing, config)` constructor with validation. `GetOrCreate` populates cache on miss. `AddMessage`/`SetSummary`/`SetHistory`/`TruncateHistory` all write-through + invalidate. `GetHistory`/`GetSummary` cache-first with backing fallback. `EvictableStore` methods delegate to backing store (type-asserted). `Ping()` for health checks. `Stats()` returns `CacheStats` (key count, prefix, TTL). `Invalidate(key)` for manual cache clearing. `ParseRedisURL()` helper creates `*redis.Client` from `redis://`/`rediss://` URLs. Corrupt cache entries auto-cleaned on unmarshal failure. 38 new tests with in-memory fake Redis (no real Redis needed): constructor (nil client/nil backing/success/defaults/custom config), DefaultConfig, GetOrCreate (cache miss populates/cache hit/backing error), AddMessage (write-through + invalidate/backing error), GetHistory (cache hit/miss/backing error), GetSummary (cache hit/miss), SetSummary (invalidates/backing error), SetHistory (invalidates/backing error), TruncateHistory (invalidates/backing error), Save/Close delegation, SessionCount (evictable/non-evictable), DeleteSession (both stores/not found/non-evictable), EvictExpired/EvictLRU (evictable/non-evictable), Ping, Stats, Invalidate, custom prefix, cached session round-trip, message conversion (with media/tool calls/reasoning), corrupt cache entry, custom context, TTL applied, multi-session isolation, ParseRedisURL (valid/empty/invalid/TLS), interface compliance (SessionStore + EvictableStore), nil session, full flow (create→add→read→summary→truncate→delete). |
| R6 | Health check improvements | P1 | ✅ Done | New `Checker` struct in `pkg/health/checker.go` with component-level health checks, dependency health tracking, and configurable timeouts. `ComponentConfig` with name, type (database/cache/messaging/external/internal), `CheckerFunc`, timeout, critical flag, and cache interval. `Register`/`Deregister` for dynamic component management. `CheckComponent` (single), `CheckAll` (concurrent), `CheckCached` (interval-aware caching). `IsReady` (critical components only) and `IsHealthy` (all components). `OverallStatus` aggregation (healthy/degraded/unhealthy). Built-in check functions in `checks.go`: `SQLiteCheck` (ping + WAL mode + DB size), `PostgreSQLCheck` (ping + connection pool stats + utilization warning at 90%), `PingerCheck` (generic for Redis etc.), `NATSCheck` (connection status), `CustomCheck` (simple pass/fail wrapper), `CompositeCheck` (aggregated sub-checks), `TimeoutCheck` (wraps any checker with timeout). New HTTP handlers in `handlers.go`: `GET /health/live` (liveness probe — always 200), `GET /health/ready` (readiness probe — 503 if critical components unhealthy, lists failed components), `GET /health/detailed` (full component-level report with status/type/critical/duration/details per component + summary counts), `GET /health/component/{name}` (single component on-demand check). `RegisterHandlers(mux)` registers all 4 endpoints. `DetailedResponse` model with overall status, uptime, timestamp, per-component `ComponentInfo`, and `HealthSummary` (total/healthy/degraded/unhealthy counts). Concurrent-safe via `sync.RWMutex`. Results cached with configurable per-component intervals. Timeouts enforced per check (default 5s). 45 new tests. |

---

## Phase 6: Hardening & Launch

### Tasks

| ID | Task | Priority | Status | Notes |
|---|---|---|---|---|
| H1 | Container-level agent sandboxing | P1 | ⬜ TODO | gVisor or Firecracker per agent execution |
| H2 | GDPR compliance toolkit | ✅ Done | Data export, right-to-deletion, retention policies |
| H3 | Load testing | P1 | ⬜ TODO | Target: 1K concurrent users, 10K total |
| H4 | Security audit (external) | P0 | ⬜ TODO | Professional pen testing |
| H5 | API documentation (OpenAPI) | P1 | ⬜ TODO | Full API spec + developer guide |
| H6 | Beta launch | P0 | ⬜ TODO | Limited rollout with monitoring |

---

## Change Log

| Date | Change |
|---|---|
| 2026-03-08 | H2 complete: GDPR compliance toolkit. New `pkg/gdpr` package providing comprehensive GDPR data subject rights implementation. **Core models**: `DataSubjectRequest` (tracks export/erasure requests through pending→processing→completed/failed/canceled lifecycle), `DataExport` (structured multi-section user data export), `ErasureReport` (itemized deletion report per data source), `RetentionPolicy` (configurable per-category data retention durations). **RequestStore**: `NewRequestStore(db)` initializes SQLite schema. Full CRUD: `Create` (auto-ID, validation for type/status, timestamps), `GetByID`, `ListByUser` (DESC by created_at), `ListByStatus` (ASC by created_at for queue processing), `UpdateStatus` (auto-sets completed_at for terminal states), `SetResult` (stores JSON export data, marks completed), `Count` (with optional status filter), `DeleteBefore` (retention cleanup for completed requests only), `Close` (no-op, caller owns DB). **UserDataSource interface**: pluggable data source abstraction with `Name()`, `Export(ctx, userID)` (returns data + count), `Erase(ctx, userID)` (returns deleted count). Enables any store (users, sessions, billing, agents, audit, integrations, vault) to participate in GDPR operations by implementing this interface. **Service**: `NewService(ServiceConfig)` with request store, data sources, and retention policy (defaults to `DefaultRetentionPolicy`: 1yr audit, 6mo usage, 3mo sessions, 30d deleted users). `ExportUserData(ctx, userID, requestedBy)` collects from all registered sources, continues on individual source errors (captured in section), stores result JSON in DSR record. `EraseUserData(ctx, userID, requestedBy)` deletes across all sources with mutex for serialization, marks DSR as failed if any source errors. `EnforceRetention(ctx, enforcer)` applies configurable retention rules via `RetentionEnforcer` interface. `GetRequest`/`ListUserRequests`/`ListPendingRequests`/`CancelRequest` for DSR management. **RetentionEnforcer**: `RetentionRule` (name, duration, cleanup function), `DefaultRetentionEnforcer` with `NewRetentionEnforcer(rules)`. **API** (5 endpoints): `POST /api/v1/gdpr/export` (data export for authenticated user), `POST /api/v1/gdpr/erase` (data erasure with `confirm:true` safety gate, irreversible), `GET /api/v1/gdpr/requests` (list user's DSRs with count), `GET/DELETE /api/v1/gdpr/requests/{id}` (view or cancel specific DSR, multi-user isolation — users can't see others' requests), `GET /api/v1/gdpr/retention` (current retention policy in days). All endpoints require auth (401), return 503 when service not configured. Migration `017_create_data_subject_requests.sql` with indexes on user_id, status, type. 68 new tests covering: ValidRequestType (4 cases), ValidRequestStatus (7 cases), DefaultRetentionPolicy, store (nil DB, OK, create success/nil/empty user/invalid type/invalid status/custom ID, get by ID/not found/empty, list by user/empty user, list by status/invalid, update status processing/completed with completed_at/failed with error/not found/empty ID/invalid, set result/not found/empty ID, count total/by status/invalid, delete before/no pending, close, persistence), service (nil store, OK, default retention, custom retention), export (success/empty user/source error continues/multiple sources/creates DSR record/no sources), erase (success/empty user/partial failure marks failed/all success marks completed), retention (single rule/error captured/nil rules/multiple rules), request management (get/list/list pending/cancel/cancel already processed/cancel not found), retention enforcer (nil receiver/empty), JSON serialization (DataExport/ErasureReport/RetentionReport), multi-user isolation, API (export unauthorized/method not allowed/no service/success, erase no confirm/success/unauthorized/method not allowed, requests list/unauthorized/empty, request by ID get/not found/wrong user/cancel/cancel already processed/empty ID, retention get/unauthorized/method not allowed, register routes), generateID uniqueness, error constants, type/status constants, concurrent reads (file-backed DB with WAL), full lifecycle (export→erase→verify DSRs). |
| 2026-03-08 | R6 complete: Health check improvements. New `Checker` struct in `pkg/health/checker.go` providing production-ready component-level health checks with dependency health tracking. `ComponentConfig` registers checks with name, type (database/cache/messaging/external/internal), `CheckerFunc`, configurable timeout (default 5s), critical flag (affects readiness), and cache interval (default 30s). `Register`/`Deregister` for dynamic component management. `CheckComponent` (single check with timeout), `CheckAll` (concurrent execution of all checks), `CheckCached` (interval-aware caching — only re-runs stale checks). `IsReady` (true if all critical components are healthy or degraded), `IsHealthy` (true if ALL components are healthy). `OverallStatus` aggregation function. Built-in check functions in `checks.go`: `SQLiteCheck` (ping + WAL mode + DB size estimation), `PostgreSQLCheck` (ping + connection pool stats + 90% utilization warning), `PingerCheck` (generic for Redis/any Pinger interface), `NATSCheck` (via NATSChecker interface), `CustomCheck` (simple pass/fail wrapper), `CompositeCheck` (aggregated sub-checks), `TimeoutCheck` (wraps any checker with custom timeout). New HTTP handlers in `handlers.go`: `GET /health/live` (liveness probe — always 200), `GET /health/ready` (readiness probe — 503 if critical components unhealthy, lists failed components), `GET /health/detailed` (full component report with status/type/critical/duration/details + HealthSummary counts), `GET /health/component/{name}` (single component on-demand). `RegisterHandlers(mux)` registers all 4 endpoints. `DetailedResponse` with overall status, uptime, timestamp, per-component `ComponentInfo`, `HealthSummary` (total/healthy/degraded/unhealthy). Concurrent-safe via `sync.RWMutex`. 45 new tests covering: NewChecker, Register (success/empty name/nil func/duplicate/defaults), Deregister, CheckComponent (success/not found/timeout), CheckAll (3 components/concurrency), CheckCached (cache hit/stale re-check), IsReady (no critical/critical healthy/degraded/unhealthy/empty), IsHealthy (true/false), ComponentNames, OverallStatus (6 cases), SQLiteCheck (success/nil DB), PostgreSQLCheck (nil DB), PingerCheck (success/fail/nil), NATSCheck (connected/disconnected/nil), CustomCheck (pass/fail), CompositeCheck (mixed/all healthy), TimeoutCheck (timeout/fast), DetailedHandler (success/unhealthy/empty), LiveHandler, ReadyHandler (ready/not ready), ComponentHandler (success/not found/unhealthy), RegisterHandlers, ConcurrentAccess, CheckResultJSON, StatusConstants, TypeConstants, DefaultConstants, CheckerWithExistingServer, CacheUpdatesOnRecheck. **Phase 5 complete.** |
| 2026-03-07 | R5 complete: Redis session cache. New `pkg/rediscache` package providing a read-through/write-through caching layer for session data using Redis. `RedisClient` minimal interface (Get/Set/Del/Ping/DBSize) — both `*redis.Client` and `*redis.ClusterClient` satisfy it naturally. `RedisCache` struct wraps any `SessionStore` (SQLite, PostgreSQL, etc.) with Redis caching. Reads check Redis first (JSON-serialized sessions with compact field names), fall through to the backing store on cache miss and populate cache. Writes always go to the backing store first (source of truth), then invalidate the cache entry. `Config` with `KeyPrefix` (default "sess:"), `TTL` (default 30min), optional base `context.Context`. `New(client, backing, config)` constructor with validation (nil client → `ErrNilClient`, nil backing → `ErrNilBackingStore`). `GetOrCreate` populates cache on miss. `AddMessage`/`SetSummary`/`SetHistory`/`TruncateHistory` all write-through + invalidate. `GetHistory`/`GetSummary` cache-first with transparent backing fallback. `EvictableStore` methods (SessionCount/DeleteSession/EvictExpired/EvictLRU) delegate to backing store via type assertion — graceful error for non-evictable stores. `Ping()` for Redis health checks. `Stats()` returns `CacheStats` (key count, prefix, TTL). `Invalidate(key)` for manual cache clearing. `ParseRedisURL(url)` helper creates `*redis.Client` from `redis://`/`rediss://` (TLS) URLs. Corrupt cache entries auto-cleaned on JSON unmarshal failure (treated as cache miss). `cachedSession`/`cachedMessage` types with short JSON field names for compact Redis storage. `toCachedMessages`/`fromCachedMessages` conversion helpers. Added `github.com/redis/go-redis/v9` dependency. 38 new tests (all using in-memory fake Redis — no real Redis server needed): constructor validation (5), DefaultConfig, GetOrCreate cache miss/hit/backing error, AddMessage write-through + invalidate/backing error, GetHistory cache hit/miss/backing error, GetSummary cache hit/miss, SetSummary invalidate/backing error, SetHistory invalidate/backing error, TruncateHistory invalidate/backing error, Save/Close delegation, SessionCount evictable/non-evictable, DeleteSession both stores/not found/non-evictable, EvictExpired/EvictLRU evictable/non-evictable, Ping, Stats, Invalidate, custom prefix, cached session JSON round-trip, message conversion with media/tool calls/reasoning, corrupt cache entry auto-clean, custom context, TTL verification, multi-session isolation, ParseRedisURL valid/empty/invalid/TLS, interface compliance (SessionStore + EvictableStore), nil session safety, full lifecycle flow. |
| 2026-03-07 | R4 complete: Kubernetes Helm chart. New `deploy/helm/operator-os/` chart with full production Kubernetes deployment support. **Chart.yaml**: v2 chart spec, app version 1.0.0. **values.yaml**: Comprehensive defaults for both gateway and worker deployments. **Templates (14 files)**: `_helpers.tpl` (8 template definitions: name, fullname, chart, labels, selectorLabels, gateway/worker selectorLabels, serviceAccountName), `configmap.yaml` (OPERATOR_LOG_FORMAT/LEVEL, HEALTH_HOST/PORT, conditional NATS_URL/REDIS_URL, worker config), `secret.yaml` (OPERATOR_ENCRYPTION_KEY, JWT_SECRET, STRIPE keys, PG_DSN, dynamic provider API keys via range), `serviceaccount.yaml` (conditional creation with annotations), `gateway-deployment.yaml` (API server + channel handlers: non-root securityContext uid=1000, liveness `/health` + readiness `/ready` probes, configMap/secret envFrom, checksum annotations for rolling restart on config change, extraEnv/volumes/nodeSelector/affinity/tolerations support), `worker-deployment.yaml` (stateless message processors: same security/probe/config pattern, conditional on `worker.enabled`), `service.yaml` (ClusterIP default, port 18790, optional NodePort), `ingress.yaml` (conditional, className, TLS, multi-host/path support), `gateway-hpa.yaml` (autoscaling/v2, CPU 70%/memory 80% targets, 2-10 replicas, scale-up 2 pods/60s with 60s stabilization, scale-down 1 pod/120s with 300s stabilization), `worker-hpa.yaml` (CPU 60%/memory 75% targets, 2-20 replicas, aggressive scale-up 4 pods/60s with 30s stabilization, scale-down 2 pods/120s with 300s stabilization), `gateway-pdb.yaml` + `worker-pdb.yaml` (policy/v1, minAvailable=1), `resourcequota.yaml` (conditional namespace-level limits: CPU/memory/pods), `servicemonitor.yaml` (conditional Prometheus ServiceMonitor for /metrics endpoint). `tests/test-connection.yaml` (helm test pod). **Design decisions**: (1) Workers get higher resource limits than gateways (1Gi vs 512Mi, 2 CPU vs 1 CPU) since they handle LLM processing. (2) Worker HPA targets lower CPU threshold (60% vs 70%) and scales to more replicas (20 vs 10) for burst handling. (3) Worker HPA scales up faster (4 pods/60s, 30s stabilization vs 2 pods/60s, 60s stabilization). (4) All external services (PostgreSQL, NATS, Redis) disabled by default — self-hosted mode works without them. (5) Secrets use stringData for readability; production should use external secret managers. (6) Pod checksum annotations ensure rolling restart when ConfigMap or Secret content changes. (7) Gateway and worker use separate selector labels (app.kubernetes.io/component) for independent scaling. 35 tests in `deploy/helm/helm_test.go` covering: Chart.yaml validation, values.yaml structure + all default values (gateway: replicas/resources/HPA/PDB, worker: enabled/concurrency/timeout/retries/HPA/PDB, service: type/port, ingress: disabled, external services: disabled, resource quota: disabled, metrics: enabled, config: log/health defaults), template file existence (14 + test), YAML validity (apiVersion/kind/metadata), helpers definitions (8), probes (both deployments), security context (non-root), configMap/secret refs, checksum annotations, HPA behavior, conditional rendering (worker/ingress/quota/servicemonitor), secret keys, configMap keys, image config, service account, resource separation, HPA scaling comparison, NATS/Redis conditional, no hardcoded secrets, command references, standard labels. |
| 2026-03-07 | R3 complete: Stateless worker architecture. New `pkg/worker` package providing horizontally-scalable, stateless message processing for Operator OS. **Worker** (`worker.go`): `Worker` struct consumes inbound messages from `bus.Bus` interface, delegates processing to an injected `MessageProcessor` function, and publishes responses back to the bus. All state operations go through external stores (SessionStore, StateStore) — the worker itself holds no conversation state. `New(cfg, bus, processor, sessions)` constructor with validation. `Run(ctx)` spawns `cfg.Concurrency` goroutines, each running `consumeLoop` that pulls from the bus and calls `processMessage`. Per-message `ProcessTimeout` (default 5m) via child context. Error responses automatically published to users on failure. `Stop()` initiates graceful shutdown via context cancellation with `CompareAndSwap` for idempotency. `Stats()` returns `WorkerStats` (ID, Running, Processed, Failed, Uptime) via atomic counters. `IsRunning()`/`IsStopped()` for health checks. Metrics integration via `metrics.RecordBusMessage()`. **Config** (`config.go`): `Config` struct with ID (required), Concurrency (default 1), ShutdownTimeout (default 30s), MaxRetries (default 3), ProcessTimeout (default 5m), HeartbeatInterval (default 30s). `DefaultConfig(id)` for sensible defaults. `validate()` enforces minimums. **Pool** (`pool.go`): `Pool` struct manages multiple workers for horizontal scaling. `NewPool(PoolConfig)` creates N workers with unique IDs (e.g., "base-0", "base-1"). `Run(ctx)` starts all workers concurrently and blocks until all stop. `Stop()` gracefully shuts down all workers. `Stats()` returns per-worker statistics. `Size()` returns pool size. Thread-safe via `sync.RWMutex`. **Design decisions**: (1) `MessageProcessor` function type decouples worker from agent loop — the existing `AgentLoop.processMessage` can be wrapped as a `MessageProcessor`, or a completely different processor can be injected. (2) Workers use the `bus.Bus` interface, so they work with both in-memory `MessageBus` (self-hosted) and `NATSBus` (SaaS) without code changes. (3) Multiple workers sharing a NATS JetStream queue get automatic work distribution via competing consumers. (4) Session state is accessed via `SessionStore` interface, so workers can share PostgreSQL-backed sessions. 25 new tests: DefaultConfig, ConfigValidate (zero config/negative concurrency/negative retries/valid preserved), New (empty ID/nil bus/nil processor/success/nil sessions OK), RunAndStop, RunAfterStop (ErrWorkerStopped), DoubleRun, ContextCancellation, ProcessesMessage (single with response verification), ProcessesMultipleMessages (5 sequential), EmptyResponse (no publish), ProcessorError (error response + stats), ProcessTimeout (100ms timeout), Stats (before/during/after run), Concurrency (4 goroutines, verifies parallel execution via maxActive tracking), Pool (invalid size/nil bus/nil processor/success/worker IDs/run+stop/stats), BusClosed (clean exit), ErrWorkerStopped, DoubleStop (idempotent), BusInterfaceCompliance. |
| 2026-03-07 | R2 complete: NATS JetStream message bus. New `Bus` interface in `pkg/bus/interface.go` defining the contract for all message bus implementations: `PublishInbound`, `ConsumeInbound`, `PublishOutbound`, `SubscribeOutbound`, `PublishOutboundMedia`, `SubscribeOutboundMedia`, `Close`. Both `MessageBus` (in-memory, self-hosted) and `NATSBus` (JetStream, SaaS) implement it with compile-time assertions. New `pkg/bus/nats_bus.go` with `NATSBus` struct: connects to NATS via `nats.go` client, creates JetStream stream `OPERATOR` (WorkQueue retention, 24h max age, file storage) with three subjects (`operator.inbound`, `operator.outbound`, `operator.outbound.media`). Durable pull consumers with explicit ack policy, configurable `AckWait` (default 30s) and `MaxDeliver` (default 5) for at-least-once delivery. `NATSConfig` with `DefaultNATSConfig(url)` for sensible defaults. `ConsumerPrefix` for multi-instance isolation (e.g., `worker-1-inbound`). JSON serialization for all message types. `publish()` uses `js.Publish()` with JetStream acknowledgment. `consume()` uses `consumer.Fetch()` with timeout-based polling loop, JSON deserialization, explicit `Ack()` on success, `Nak()` on unmarshal errors for redelivery. Connection resilience: `MaxReconnects(-1)`, `ReconnectWait(2s)`, disconnect/reconnect logging handlers. `Close()` drains NATS connection for graceful shutdown. `Conn()` and `Stream()` accessors for health checks/diagnostics. Metrics integration via `metrics.RecordBusMessage()`. 34 new tests: `DefaultNATSConfig` (defaults, custom values), `NewNATSBus` (empty URL, invalid URL), interface compliance, subject constants, JSON round-trip (InboundMessage, OutboundMessage, OutboundMediaMessage, MediaPart, Peer, SenderInfo), `MessageBus` via `Bus` interface (publish/consume all 3 channels, close), config defaults (stream name, ack wait, max deliver), consumer naming with prefix, existing MessageBus behavior preserved, closed publish all methods, canceled context all methods, `ErrBusClosed`, default constants. |
| 2026-03-07 | R1 complete: PostgreSQL session/state/credential stores (SaaS mode). New `pkg/pgstore` package providing PostgreSQL connection pooling via `pgx/v5` stdlib driver. `Config` struct with DSN, MaxOpenConns (25), MaxIdleConns (10), ConnMaxLifetime (5m), ConnMaxIdleTime (1m). `DefaultConfig(dsn)` for sensible defaults. `Open(cfg)` creates pooled `*sql.DB` with ping verification. New `PGStore` in `pkg/session/pg_store.go` implements both `SessionStore` and `EvictableStore` interfaces with full parity to SQLiteStore: GetOrCreate, AddMessage, GetHistory, GetSummary, SetSummary, SetHistory, TruncateHistory, Save (no-op), SessionCount, DeleteSession, EvictExpired, EvictLRU, Close (no-op — caller owns DB). PostgreSQL schema uses TIMESTAMPTZ for timestamps, BIGSERIAL for message IDs, ON DELETE CASCADE for messages. All queries use $N positional parameters. Native `time.Time` scanning (no string parsing needed). New `PGStateStore` in `pkg/state/pg_store.go` implements `StateStore`: Get, Set (UPSERT), GetTimestamp (native time.Time), Close (no-op). New `PGCredentialStore` in `pkg/auth/pg_credential_store.go` implements `CredentialStore`: Get, Set, Delete, DeleteAll, List, Close (no-op). Uses BYTEA for encrypted data, BOOLEAN for encrypted flag. Shares AES-256-GCM encryption with `encryptAESGCM`/`decryptAESGCM` from SQLite store. Base64 fallback when no encryption key. Shared `*sql.DB` ownership model: pgstore.Open() creates the pool, stores receive it via constructor, Close() is a no-op on stores (pool lifecycle managed by caller). 43 new tests: pgstore_test.go (3) — DefaultConfig, OpenEmptyDSN, OpenInvalidDSN; pg_store_test.go (20) — NilDB, InterfaceCompliance, GetOrCreate, AddAndGetHistory, AddMessageWithToolCalls, Summary, SummaryNotFound, SetSummaryNotFound, SetHistory, TruncateHistory, TruncateAll, Save, SessionCount, DeleteSession, DeleteSessionNotFound, EvictExpired, EvictLRU, EvictLRUZero, Close, EmptyHistory, MessageWithMedia; pg_store_test.go (state, 9) — NilDB, InterfaceCompliance, GetSet, Upsert, Timestamp, Close, MultipleKeys; pg_credential_store_test.go (11) — NilDB, InterfaceCompliance, SetGet, GetNotFound, Upsert, Delete, DeleteAll, List, Unencrypted, Close. Integration tests gated behind `OPERATOR_TEST_PG_DSN` env var. **Phase 5 started.** |
| 2026-03-07 | S8 complete: Per-agent scope narrowing. New `AgentIntegrationScope` model in `pkg/agents/store.go` with `IntegrationID`, `AllowedTools`, `AllowedScopes` fields. New `pkg/integrations/scope_filter.go` with `AgentScopeFilter` struct: `NewAgentScopeFilter(scopes)` creates filter (nil/empty = open access, all integrations allowed). Methods: `IsOpenAccess()`, `IsIntegrationAllowed(id)`, `IsToolAllowed(integrationID, toolName)`, `IsScopeAllowed(integrationID, scopeName)`, `FilterTools([]*IntegrationTool)` (bulk filter by integration+tool), `FilterToolNames(names, registry)` (filter using registry lookup), `AllowedIntegrationIDs()` (sorted list), `GetScope(id)` (scope config for integration), `EffectiveScopes(id, defaults)` (intersection of allowed and default OAuth scopes). `ValidateAgentScopes(scopes, registry)` validates: required fields, duplicate integration IDs, integration existence in registry, tool name validity, scope name validity against OAuth config. `ScopeSummary(scopes)` for human-readable display. `UserAgent` model updated with `AllowedIntegrations []AgentIntegrationScope` field. SQLite store: added `allowed_integrations TEXT NOT NULL DEFAULT '[]'` column to `user_agents` schema. `marshalIntegrationScopes`/`unmarshalIntegrationScopes` JSON helpers. Create, Update, GetByID (via `getBy`), ListByUser (via `scanAgent`), GetDefault all read/write the field. Agent API: `CreateAgentRequest`/`UpdateAgentRequest` (pointer `*[]AgentIntegrationScope` for partial updates)/`AgentResponse` all include `allowed_integrations`. Validation on create/update: integration_id required per scope entry. `agentToResponse` includes field. Migration `016_add_agent_integrations.sql` adds column. 47 new tests: scope_filter_test.go (37) — Validate (valid, empty, with tools/scopes), NewAgentScopeFilter (nil, empty, restricted), IsIntegrationAllowed (open/allowed/disallowed), IsToolAllowed (open/not allowed integration/all tools/specific tools), IsScopeAllowed (open/not allowed/all scopes/specific), FilterTools (open/by integration/by tools/none allowed), FilterToolNames (open/by integration/by tools/unknown/nil registry), AllowedIntegrationIDs (open/sorted), GetScope (open/allowed/disallowed), EffectiveScopes (open/not allowed/empty/intersection/excluded), ValidateAgentScopes (nil/empty/valid/missing id/duplicate/not in registry/valid with registry/invalid tool/invalid scope), ScopeSummary (empty/all tools/specific/multiple), MultiIntegration, DuplicateOverwrites; sqlite_store_test.go (10) — CreateAndGet, EmptySlice, Update, ListByUser, GetDefault, Persistence, MarshalUnmarshalIntegrationScopes (3 cases), UnmarshalIntegrationScopes (5 cases). **Phase 4 complete.** |
| 2026-03-07 | S7 complete: Integration management API. New `pkg/integrations/management_api.go` with `ManagementAPI` struct orchestrating `IntegrationRegistry`, `UserIntegrationStore`, `oauth.Service`, `oauth.VaultStore`, and `oauth.TokenRefreshManager` for full integration lifecycle management. `ManagementAPIConfig` for dependency injection. 8 REST endpoints under `/api/v1/manage/integrations/`: **Connect** (`POST /connect`): supports OAuth 2.0 (initiates authorization flow via `oauth.Service.StartFlow`, creates pending user integration record, returns `auth_url`), API key (stores key in encrypted vault, marks active immediately), and no-auth (direct activation). Handles reconnecting previously failed/revoked/disabled integrations by reusing existing records. **Disconnect** (`POST /disconnect`): revokes vault tokens, resets refresh manager retry state, deletes user integration record and vault credential. **List Status** (`GET /status`): returns all user integrations enriched with `IntegrationStatus` containing registry metadata (name, category, auth_type), `TokenStatus` (has_access_token, has_refresh_token, expires_at, is_expired, needs_refresh, token_status from vault), and `RefreshStatus` (retry count, max retries, last error, next retry time, exhausted flag from refresh manager). Supports `?status=` filter. **Single Status** (`GET /{id}/status`): same enrichment for one integration. **Enable** (`POST /{id}/enable`): re-enables disabled integrations (validates state transition: only disabled→active). **Disable** (`POST /{id}/disable`): pauses integrations without disconnecting (preserves vault tokens; validates: only active→disabled). **Reconnect** (`POST /{id}/reconnect`): re-initiates OAuth flow for failed/revoked integrations, resets refresh retries, uses existing scopes as fallback, optional request body for new scopes/redirect. Only available for OAuth integrations. **Update Config** (`PUT /{id}/config`): merges new config key-value pairs into existing integration config. Models: `ConnectRequest` (integration_id, config, scopes, redirect_after, api_key), `ConnectResponse` (integration_id, status, auth_url, message), `IntegrationStatus` (enriched view), `TokenStatus` (vault health). All endpoints require authentication (401), return 503 when store not configured. Multi-user isolation enforced. 45 new tests covering: constructor, RegisterRoutes, connect (unauthorized, no store, missing ID, not found, already connected, OAuth no service/success/reconnect existing, API key missing/success/no vault, no-auth success, method not allowed, invalid JSON), disconnect (success with vault cleanup, not found, unauthorized, missing ID, method not allowed, no vault), list status (empty, with integrations + token enrichment, unauthorized, status filter), single status (success, not found), enable (success, already active, invalid state, not found), disable (success, already disabled, invalid state), reconnect (success, not OAuth, not found, no OAuth service), update config (success, not found, missing config, invalid JSON), unknown action, invalid path, wrong methods, multi-user isolation, buildStatus (with token status, expired token, no vault), context helpers. |
| 2026-03-07 | S6 complete: Shopify integration. New `pkg/integrations/shopify` package providing declarative integration manifests and tool definitions for three Shopify Admin API domains. **Shopify Products** (7 tools): `shopify_list_products` (with status/vendor/type/collection filters, cursor pagination), `shopify_get_product` (full product details with variants), `shopify_create_product` (title, HTML description, variants with pricing/SKU/inventory, tags, status), `shopify_update_product` (partial update), `shopify_delete_product` (permanent deletion), `shopify_count_products` (filtered counts), `shopify_search_products` (Shopify search syntax). Scopes: read_products, write_products. **Shopify Orders** (7 tools): `shopify_list_orders` (status/financial/fulfillment filters, date range, cursor pagination), `shopify_get_order` (complete order data), `shopify_create_order` (line items, customer, notes, tags), `shopify_update_order` (note/tags/email updates), `shopify_count_orders` (filtered counts), `shopify_close_order`, `shopify_cancel_order` (reason, restock, notification options). Scopes: read_orders, write_orders. **Shopify Customers** (7 tools): `shopify_list_customers` (date filters, cursor pagination), `shopify_get_customer` (full details with addresses), `shopify_create_customer` (name, email, phone, addresses, marketing consent), `shopify_update_customer` (partial update), `shopify_search_customers` (Shopify search syntax), `shopify_count_customers`, `shopify_customer_orders` (cross-scope: requires read_customers + read_orders). All 21 tools have complete JSON Schema parameters with types/descriptions/enums/required fields, `RequiredScopes` for scope-based access control, and per-tool `RateLimit` (10-20 RPM). OAuth 2.0 with `DynamicParams` for `{shop}` substitution in authorization and token URLs (no PKCE — Shopify doesn't support it). `NewOAuthProvider(clientID, clientSecret, redirectURL, shop)` creates shop-specific OAuth provider. All integrations categorized as "ecommerce" with `required_plan: starter`. `RegisterAll(registry)` batch-registers all three integrations. `ResolveEndpoint(integrationID, toolName, args)` maps tool names to HTTP method + Admin API path with `{param}` substitution and `url.PathEscape`. `ResolveBaseURL(shop)` returns shop-specific API base URL. `GetEndpointSpec()` for endpoint introspection. `NewShopifyHTTPExecutor()` wires into `integrations.NewHTTPToolExecutor` with shop-aware base URL resolution. API version pinned to `2024-10`. 40 new tests covering: OAuth provider creation + shop-specific URLs, all 3 integration manifests (fields, OAuth config, scopes, status, dynamic params, validation), tool definitions (names, descriptions, parameters, required scopes, rate limits), uniqueness across integrations, parameter type/required field checks, write tools require write scopes, cross-scope (customer_orders), registry integration (register all, categories, list by category, tool lookup, all tool names, duplicate error), endpoint resolution (Products 7 endpoints, Orders 7 endpoints, Customers 7 endpoints, unknown tool, unresolved placeholders, path escaping), base URL resolution (normal + empty), endpoint spec lookup, constants, scope constants, API version, URL templates, validation mutation, versioning, required plan, HTTP executor creation. |
| 2026-03-07 | S5 complete: First integrations — Google (Gmail, Drive, Calendar). New `pkg/integrations/google` package providing declarative integration manifests and tool definitions for three Google Workspace services. **Gmail** (7 tools): `gmail_list_messages` (inbox listing with label/pagination), `gmail_get_message` (full message content with format options), `gmail_send_message` (compose with to/cc/bcc/reply/thread support), `gmail_search_messages` (Gmail query syntax), `gmail_list_labels`, `gmail_modify_labels` (add/remove for read/unread/star/archive), `gmail_trash_message`. Scopes: gmail.readonly, gmail.send, gmail.modify, gmail.labels. **Google Drive** (7 tools): `drive_list_files` (folder listing with sort/pagination), `drive_get_file` (metadata with permissions/sharing), `drive_search_files` (Drive query syntax), `drive_get_content` (text export with size limit for Google Docs), `drive_create_file` (text content with MIME type), `drive_create_folder`, `drive_delete_file` (trash). Scopes: drive.readonly, drive.file. **Google Calendar** (7 tools): `calendar_list_events` (upcoming events with time range/search/recurrence expansion), `calendar_get_event` (full details with attendees/conferencing), `calendar_create_event` (timed/all-day with attendees/reminders/recurrence/timezone), `calendar_update_event` (partial update), `calendar_delete_event`, `calendar_list_calendars`, `calendar_quick_add` (natural language). Scopes: calendar.readonly, calendar.events. All 21 tools have complete JSON Schema parameters with types/descriptions/enums/required fields, `RequiredScopes` for scope-based access control, and per-tool `RateLimit` (10-30 RPM). `NewOAuthProvider(clientID, clientSecret, redirectURL)` creates Google OAuth 2.0 provider with PKCE, `access_type=offline`, `prompt=consent` for reliable refresh tokens. `RegisterAll(registry)` batch-registers all three integrations. `ResolveEndpoint(integrationID, toolName, args)` maps tool names to HTTP method + API path with `{param}` substitution from args, `url.PathEscape` for special characters, and default `calendar_id=primary`. `ResolveBaseURL()` returns per-integration API base URL (gmail.googleapis.com, www.googleapis.com). `GetEndpointSpec()` for endpoint introspection. `NewGoogleHTTPExecutor()` wires into `integrations.NewHTTPToolExecutor` for production use. `AllToolNames()` returns all 21 tool names. 35 new tests covering: OAuth provider creation + validation, all 3 integration manifests (fields, OAuth config, scopes, status, validation), tool definitions (names, descriptions, parameters, required scopes, rate limits), uniqueness across integrations, parameter type/required field checks, registry integration (register all, categories, list by category, tool lookup, all tool names), tool adapter (execution, error, no executor), endpoint resolution (Gmail 6 endpoints, Drive 5 endpoints, Calendar 8 endpoints, unknown tool, unresolved placeholders, path escaping), base URL resolution, endpoint spec lookup, constants, scope constants, validation mutation, versioning, required plan, registrar iteration, Gmail send required fields, Calendar create required fields. |
| 2026-03-07 | S4 complete: Token refresh manager. New `pkg/oauth/refresh_manager.go` with `TokenRefreshManager` for automatic OAuth token refresh before expiry. Background sweep loop via `Start()`/`Stop()` checks vault for expiring credentials at configurable intervals (default 5min). Per-credential singleflight mutex prevents concurrent refresh attempts for the same user+provider pair. `doRefresh()` re-reads from vault after acquiring lock to skip redundant refreshes when another goroutine already refreshed. Exponential backoff retry system: configurable `MaxRetries` (default 3) and `RetryDelay` (default 30s) with delay doubling per attempt (30s → 60s → 120s). `refreshState` tracks per-credential retry count, last error, and next retry time. `EnsureFresh(userID, providerID)` for on-demand token freshness — returns credential immediately if not expiring, otherwise triggers refresh. `RefreshCredential(userID, providerID)` for forced manual refresh. `GetRefreshStatus(userID, providerID)` returns `RefreshStatus` with retry count, max retries, last error, next retry time, exhausted flag. `ResetRetries(userID, providerID)` clears retry state for manual recovery. `RefreshConfig` with `CheckInterval`, `RefreshBefore`, `MaxRetries`, `RetryDelay`, `OnRefresh`/`OnError` callbacks. `DefaultRefreshConfig()` returns sensible defaults. `SweepableVault` interface extends `VaultStore` with `ListExpiring(before time.Time)` for efficient expiry queries. `SQLiteVaultStore.ListExpiring()` queries active credentials with non-empty `expires_at` before threshold, sorted by `expires_at ASC`. 30 new tests covering: constructor (nil vault, nil service, default config, custom config), DefaultRefreshConfig, Start/Stop (double stop safety), EnsureFresh (empty user/provider, not found, not expiring, no refresh token, no expiry), RefreshCredential (empty user/provider, not found, no refresh token), GetRefreshStatus (none, with retries, exhausted), ResetRetries, ListExpiring (empty, finds expiring, skips non-active, multiple sorted), SweepableVault interface compliance, maybeRefresh (respects max retries, respects backoff), concurrent singleflight, OnError callback, credKey, errorString, checkAll with non-sweepable vault, multi-user isolation, exponential backoff timing, doRefresh re-reads from vault, doRefresh deleted credential, getCredMutex same/different keys. |
| 2026-03-07 | S3 complete: Integration registry (declarative manifests). New `pkg/integrations` package providing declarative integration manifests with automatic tool registration. `Integration` model with full manifest support: ID, name, icon, category, description, auth_type (oauth2/api_key/none), `OAuthConfig` (authorization_url, token_url, scopes, use_pkce, dynamic_params for user-configurable URL templates like Shopify's {shop}, extra_auth_params), `APIKeyConfig` (header, prefix, label), `ToolManifest` array (name, description, JSON Schema parameters, required_scopes, rate_limit), required_plan, suggested_templates, status (active/beta/deprecated), version. `Validate()` enforces required fields, auth type consistency (oauth2 requires OAuthConfig, api_key requires APIKeyConfig), and unique tool names within integration. `IntegrationRegistry` with `sync.RWMutex` concurrent-safe operations: `Register` (validates + rejects duplicates), `Replace` (validates + upserts for hot-reload), `Get`, `List` (sorted by ID), `ListByCategory` (case-insensitive), `ListActive` (active + default status), `Remove`, `Count`, `Categories` (sorted unique). `LoadFromJSON` / `LoadMultipleFromJSON` for declarative manifest loading from JSON files/configs (stops on first error). `GetToolManifest(toolName)` returns tool + parent integration ID across all integrations. `AllToolNames()` returns sorted global tool list. `UserIntegrationStore` interface + `SQLiteUserIntegrationStore` backend for per-user integration state. `UserIntegration` model with id, user_id, integration_id, status (pending/active/failed/revoked/disabled), config (map[string]string for integration-specific settings), scopes (granted OAuth scopes), error_message, last_used_at, timestamps. CRUD: `Create` (auto-ID, default pending status, unique constraint on user+integration), `Get(userID, integrationID)`, `GetByID(id)`, `ListByUser(userID, status)` with optional status filter, `Update` (full record), `UpdateStatus` (status + error message), `Delete`, `RecordUsage` (updates last_used_at), `CountByUser`. `IntegrationTool` adapter implements `tools.Tool` interface, wrapping `ToolManifest` and delegating execution to `IntegrationToolExecutor` function signature. `IntegrationID()` and `RequiredScopes()` accessors. `ToolRegistrar` bridges `IntegrationRegistry` → `tools.ToolRegistry`: `RegisterAll()` registers tools from all integrations, `RegisterIntegration(id)` registers tools from one. `NewHTTPToolExecutor` creates a generic REST executor with configurable `BaseURLResolver`, `TokenResolver`, `EndpointResolver`. Context helpers `WithUserID`/`userIDFromContext`. `API` with 5 REST endpoints: `GET /api/v1/integrations` (list active integrations, filtered by category, sanitized summaries — no OAuth secrets exposed), `GET /api/v1/integrations/categories` (unique sorted categories), `GET /api/v1/integrations/{id}` (single integration summary), `GET/POST /api/v1/user/integrations` (list user's connected integrations with status filter / connect new integration with registry validation, returns 409 on duplicate), `GET/DELETE /api/v1/user/integrations/{id}` (get or disconnect). All user endpoints require auth (401), return 503 when store not configured. Migration `015_create_user_integrations.sql` with indexes on user_id, (user_id, status), integration_id, and UNIQUE(user_id, integration_id). 80 new tests covering: validation (17 cases: missing fields, invalid auth type/status, OAuth missing config/URLs, API key missing config/header, empty/duplicate tool names, default status, none auth, API key success), ValidAuthType/ValidIntegrationStatus, ToolNames, registry (new/register/nil/invalid/duplicate, replace/nil/invalid, get/not found, list sorted, list by category/case-insensitive, list active, remove/not found, categories), JSON loading (single/invalid/bad manifest, multiple/invalid/stops on error), tool lookup (get tool manifest/not found, all tool names), JSON roundtrip with dynamic params, store (constructor nil/ok, create success/nil/empty fields/invalid status/duplicate/custom ID/with scopes, get/not found/empty fields, get by ID/not found/empty, list by user/status filter/invalid/empty, update/nil/empty ID/not found/invalid status, update status/with error/not found/invalid/empty fields, delete/not found/empty fields, record usage/not found/empty, count/empty, multi-user isolation, close, full lifecycle, valid statuses), tool adapter (implements interface, name/description/parameters/integration ID/scopes, execute no executor/success/error/passes args), registrar (register all/single/not found), context helpers, API (list/by category/method not allowed, categories, get/not found, user list empty/unauthorized, create success/missing ID/not found/duplicate/invalid JSON/unauthorized, user get/not found, delete/not found, no store, register routes). |
| 2026-03-07 | S2 complete: Encrypted credential vault. New `pkg/oauth/vault.go` with `VaultStore` interface + `SQLiteVaultStore` backend providing per-user per-integration encrypted token storage. `VaultCredential` model with user_id, provider_id, access_token, refresh_token, token_type, id_token, scopes, label, status (active/revoked/expired), expires_at. AES-256-GCM encryption with Argon2id key derivation (matching `pkg/auth` parameters). Base64 fallback when no encryption key set (with warning). `Store()` upserts by (user_id, provider_id) unique constraint with auto-generated IDs, status validation. `Get(userID, providerID)` retrieves and decrypts a credential. `GetByID(id)` retrieves by unique ID. `ListByUser(userID, status)` lists all credentials for a user with optional status filter. `Delete(userID, providerID)` and `DeleteByID(id)` with not-found error. `Revoke(userID, providerID)` marks credential as revoked. `DeleteExpired()` removes credentials with expired tokens (skips those with no expiry). `StoreFromTokenResponse(store, resp, label)` convenience converts OAuth `TokenResponse` to `VaultCredential` and stores it. `vaultEncryptAESGCM`/`vaultDecryptAESGCM` private encryption helpers (salt + nonce + ciphertext format). `ValidCredentialStatus()` validation. `IsExpired()`/`NeedsRefresh()` on `VaultCredential`. Migration `014_create_credential_vault.sql` with indexes on user_id, provider_id, (user_id, status), and UNIQUE(user_id, provider_id). 45 new tests covering: constructor (nil DB, OK, no key), ValidCredentialStatus, IsExpired/NeedsRefresh, Store (success, nil, empty user/provider/token, invalid status, upsert, with expiry, custom ID), Get (success, not found, empty user/provider), GetByID (success, not found, empty), ListByUser (success, status filter, invalid status, empty, empty user), Delete (success, not found, empty user/provider), DeleteByID (success, not found, empty), Revoke (success, not found, empty user/provider), DeleteExpired (success, none), multi-user isolation, encrypted/unencrypted modes, encryption key mismatch, StoreFromTokenResponse (success, nil store/response, missing user/provider), encrypt/decrypt helpers (round-trip, too short, wrong key), close, full flow (store→get→update→list→revoke→delete). |
| 2026-03-07 | S1 complete: OAuth 2.0 framework (PKCE). New `pkg/oauth` package providing a generic, multi-tenant OAuth 2.0 authorization framework with PKCE (RFC 7636) support. `Provider` model with declarative configuration (ID, name, auth/token URLs, client credentials, scopes, redirect URL, PKCE flag, extra auth params). `Provider.Validate()` for config validation. `ProviderRegistry` for managing multiple OAuth providers with Register/Get/List/Remove. `OAuthState` model for tracking in-flight authorization flows (user_id, provider_id, CSRF state token, PKCE code_verifier, redirect_uri, scopes, expiry, used flag). `StateStore` interface + `SQLiteStateStore` backend with Create (auto-ID, auto-timestamps, validation), GetByState (CSRF lookup), MarkUsed (replay prevention), DeleteExpired (cleanup). `TokenResponse` model for parsed OAuth token responses. `Service` orchestrates the full OAuth flow: `StartFlow(userID, providerID, scopes, redirectAfter)` generates CSRF state + PKCE codes, merges default and requested scopes, persists state, builds authorization URL; `HandleCallback(state, code)` validates state (expiry, replay), marks used, exchanges code for tokens via provider's token endpoint; `RefreshToken(providerID, refreshToken)` exchanges refresh tokens with preserved-token logic. `parseTokenResponse` handles standard OAuth responses including error responses (`error`/`error_description`). `API` with 4 REST endpoints: `GET /api/v1/oauth/providers` (list available providers, sanitized — no secrets exposed), `POST /api/v1/oauth/authorize` (initiate flow, requires auth), `GET /api/v1/oauth/callback` (process provider callback with error/state/code handling), `POST /api/v1/oauth/refresh` (refresh tokens, requires auth). Reuses existing `pkg/auth.GeneratePKCE()` for code verifier/challenge generation. Migration `013_create_oauth_states.sql` with indexes on state, user_id, expires_at. 40 new tests covering: provider validation (6 field cases), scope string, registry CRUD (register/get/list/remove/duplicate/invalid/multiple), state store (nil DB, create/validation/get/mark used/delete expired/close/custom TTL/duplicate), service (nil registry/nil store/OK), start flow (empty user/empty provider/not found/success/extra scopes/extra auth params/no PKCE), handle callback (empty state/empty code/invalid state/expired/used/success/replay prevention), refresh token (empty provider/empty token/not found/success/preserves token/client secret/provider error), parse response (OAuth error/no access token/invalid JSON/expires_at), multi-user isolation, API (nil service, list empty/with providers, authorize unauthorized/invalid JSON/missing provider/not found/success, callback missing state/missing code/provider error/invalid state/success, refresh unauthorized/invalid JSON/missing provider/missing token/not found/success, register routes). **Phase 4 started.** |
| 2026-03-07 | B6 complete: Plan upgrade/downgrade. New `pkg/billing/plan_change.go` with `PlanChangeService` for handling plan upgrades and downgrades. `PlanChangeDirection` enum (upgrade/downgrade/same) with `ComparePlans()` using `PlanTierOrder()` (free=0, starter=1, pro=2, enterprise=3). `PlanChangeMode` enum (immediate/at_period_end) with `DefaultChangeMode()` (upgrades=immediate, downgrades=at_period_end). `PlanChangeRequest` (plan_id, interval, mode) and `PlanChangeResult` (direction, mode, previous_plan, new_plan, effective_at, subscription, proration_amount). `NewPlanChangeService(PlanChangeConfig)` with SubStore, Catalogue, PriceMap, Stripe client. `ChangePlan(userID, req)` handles 3 scenarios: (1) free→paid: creates new subscription with monthly/yearly period, (2) paid→paid: updates subscription with proration estimate, (3) paid→free: cancels immediately or at period end. `estimateProration()` calculates (new_price - old_price) × remaining_period_fraction in cents. `stripeUpdateSubscription()` fetches item ID, swaps price, configures proration_behavior. Self-hosted mode: downgrades without Stripe apply immediately. `Preview(userID, req)` returns projected result without mutations. `OnPlanChanged` callback for audit/notification hooks. `PlanChangeAPI` with 2 REST endpoints: `POST /api/v1/billing/change-plan` (error codes: 409 already_on_plan, 400 invalid_plan/missing_plan_id/invalid_mode/no_subscription, 404 plan_not_found, 502 stripe_error), `POST /api/v1/billing/preview-change`. 42 new tests covering: PlanTierOrder (all tiers + invalid), ComparePlans table-driven (9 cases), DefaultChangeMode, service creation (nil store/nil catalogue/success), ChangePlan (empty user/invalid plan/not found/inactive/already on plan/invalid mode), upgrade from free (monthly/yearly), upgrade paid→paid (proration positive), downgrade paid→paid (proration negative, self-hosted immediate), downgrade to free (immediate cancel/at period end/no subscription), explicit mode override, OnPlanChanged callback, multi-user isolation, proration (upgrade/downgrade/period ended/nil plans), Preview (empty user/invalid plan/upgrade/downgrade/same/explicit mode), API (unauthorized/invalid JSON/missing plan_id/upgrade success/already on plan/invalid plan), preview API (unauthorized/success/missing plan_id/invalid JSON), RegisterRoutes, interval defaulting (monthly default/preserves yearly). **Phase 3 complete.** |
| 2026-03-07 | B5 complete: Overage handling. New `pkg/billing/overage.go` with `OverageEnforcer` — 4-level progressive enforcement system for plan limit overages. `OverageThresholds` struct with configurable warning (80%), soft_cap (90%), hard_cap (100%), and block_at (120%) percentages, validated ordering. `OverageLevel` enum: `none`/`warning`/`soft_cap`/`hard_cap`/`blocked`. `OverageAction` enum: `none`/`warn`/`downgrade_model`/`throttle`/`block`. `ThrottleConfig` with fallback_model (default gpt-4o-mini), delay_ms (default 500ms), reduced_rate_percent (default 50%). `OverageEnforcerConfig` for dependency injection. `CheckTokens(userID, periodStart, plan)` checks token usage against plan limit. `CheckMessages(userID, periodStart, plan)` checks message count. `CheckAll` returns the most severe status across both resources. `CheckUser(userID)` resolves plan from subscription store and checks all limits. `GetFullStatus(userID)` returns status for all resources. `evaluate(resource, usage, limit)` computes level/action/message based on usage percentage vs thresholds. `resolvePlan(userID)` resolves user's plan and billing period from subscription. `OverageMiddleware` HTTP middleware: adds `X-Overage-Level`, `X-Overage-Action`, `X-Overage-Message`, `X-Overage-Fallback-Model` headers to all responses; blocks requests (429 with `overage_blocked` code) when at block level; adds artificial delay at hard cap level; fails open on errors (availability over strictness). `OverageAPI` with `GET /api/v1/billing/overage` returning overall_level, overall_action, and per-resource status array. 36 new tests covering: threshold defaults/validation (7 cases), enforcer creation (nil stores, invalid thresholds, success, custom config), evaluate at all levels (none/warning/soft_cap/hard_cap/blocked/way_over/exact_boundaries), CheckTokens (unlimited/nil_plan/under_limit/over_limit), CheckMessages (unlimited/nil_plan/warning), CheckAll (returns most severe, both unlimited), CheckUser (free plan, with subscription), GetFullStatus, levelSeverity, middleware (no auth/under limit/blocked/throttled), API (unauthorized/success/with_usage/register_routes), DefaultThrottleConfig, multi-user isolation. |
| 2026-03-07 | B4 complete: Usage dashboard API. New `pkg/billing/usage_api.go` with `UsageAPI` struct providing 5 REST endpoints for the usage dashboard. `GET /api/v1/billing/usage` returns aggregate `UsageSummary` for the current billing period (subscription-aware: uses `CurrentPeriodStart`/`CurrentPeriodEnd` from active subscription, falls back to calendar month for free users; supports explicit `since`/`until` query params). `GET /api/v1/billing/usage/daily` returns `[]*DailyUsage` breakdown with configurable lookback via `days` param (default 30, max 365) or explicit `since`/`until`. `GET /api/v1/billing/usage/models` returns `[]*ModelUsage` per-model breakdown sorted by total tokens DESC. `GET /api/v1/billing/usage/events` returns paginated raw `[]*UsageEvent` with filters: `model`, `since`, `until`, `limit`, `offset`. `GET /api/v1/billing/usage/limits` returns current usage vs plan limits — resolves user's plan from subscription store (defaults to Free), computes `tokens.used`/`tokens.limit`/`tokens.unlimited` and `messages.used`/`messages.limit`/`messages.unlimited` for the billing period. All endpoints require auth (401 Unauthorized), return 503 when usage store not configured. `NewUsageAPI(usageStore, subStore, catalogue)` constructor. `RegisterRoutes(mux)` registers all 5 routes. Helper functions: `parsePeriod` (subscription period → calendar month fallback), `parseTimeRange` (since/until/days params), `beginningOfMonth`. 27 new tests covering: summary (success, time range filter, unauthorized, no store, with subscription period), daily (success, days param, empty, unauthorized, no store), models (success, empty, unauthorized, no store), events (list, model filter, pagination, time range, unauthorized, no store), limits (free plan, with subscription, unauthorized, no store), multi-user isolation, beginningOfMonth, RegisterRoutes. |
| 2026-03-07 | B3 complete: Token usage metering. New `pkg/billing/usage.go` with `UsageStore` interface + `SQLiteUsageStore` backend. `UsageEvent` model tracks per-request LLM token consumption: user_id, model, provider, input/output/total tokens, session_key, agent_id, duration_ms, estimated_cost. Auto-computes `total_tokens = input + output` when not explicitly set (supports explicit override for reasoning tokens). `Record()` with validation (required ID, user_id, model) and duplicate prevention. `GetSummary(userID, since, until)` returns aggregate `UsageSummary` (total input/output/tokens/requests/cost). `GetByModel(userID, since, until)` returns `[]*ModelUsage` breakdown sorted by total tokens DESC. `GetDaily(userID, since, until)` returns `[]*DailyUsage` aggregation using `SUBSTR(created_at, 1, 10)` for SQLite Go driver compatibility (DATE() doesn't parse Go time format). `GetCurrentPeriodUsage()` and `GetCurrentPeriodMessages()` for billing period enforcement against plan limits. `ListEvents(UsageQuery)` with filters (user_id, model, since/until, limit/offset) with default limit 100, max 1000, ordered by created_at DESC. `DeleteBefore(timestamp)` for retention cleanup. Migration `012_create_usage_events.sql` with indexes on user_id, (user_id, created_at), (user_id, model), created_at. Updated dbmigrate test (11→12 migrations). 30 new tests covering: store creation (nil DB, OK), record (success, auto total, nil event, empty ID/user/model, duplicate, preserved explicit total), summary (aggregate, empty, time range), by-model (breakdown, empty), daily (aggregation, empty), period usage/messages (with before-period exclusion), list events (all, model filter, pagination, default limit, time range), delete before (success, none), multi-user isolation, persistence (reopen DB), close, limit enforcement integration (CheckLimit with plan limits). |
| 2026-03-07 | B2 complete: Stripe integration. New `pkg/billing/stripe.go` with `StripeClient` — lightweight HTTP-based Stripe API client (no SDK dependency, keeps CGO_ENABLED=0). Uses `net/http` + form-encoded params + Basic auth. Methods: `CreateCustomer(email, userID)`, `GetCustomer(id)`, `CreateCheckoutSession(params)` (subscription mode, trial support), `GetSubscription(id)`, `CancelSubscription(id)` (at period end), `CancelSubscriptionImmediately(id)`, `CreateBillingPortalSession(customerID, returnURL)`. Stripe API types: `StripeCustomer`, `StripeCheckoutSession`, `StripeSubscription` (with Items/Price), `StripeBillingPortalSession`, `StripeEvent`, `StripeError` (implements `error`). Stripe-Version header pinned to `2024-12-18.acacia`. Configurable base URL for testing. New `pkg/billing/webhook.go` with `WebhookHandler` (implements `http.Handler`). HMAC-SHA256 webhook signature verification (`VerifyWebhookSignature`) with timestamp tolerance (default 300s), multiple v1 signature support, and proper Stripe signing format (`t=TIMESTAMP.PAYLOAD`). Event routing: `checkout.session.completed` (creates/updates subscription, resolves plan from price map, handles existing subscription upgrade), `customer.subscription.updated` (syncs plan/status/period/cancel-at-period-end from Stripe), `customer.subscription.deleted` (marks canceled), `invoice.payment_failed` (marks past_due), `invoice.payment_succeeded` (recovers from past_due → active). Status mapping: Stripe `active/trialing/past_due/canceled/unpaid/paused/incomplete_expired` → internal statuses. `PlanPriceMap` for bidirectional Plan↔Stripe Price ID mapping with `SetPrice`/`GetPrice`/`PlanForPrice`. `StripeAPI` with REST endpoints: `POST /api/v1/billing/checkout` (validates plan, interval, price config, URLs; creates Stripe Checkout Session with customer reuse), `POST /api/v1/billing/portal` (creates Billing Portal session for subscription management), `GET /api/v1/billing/subscription` (returns current subscription + plan or free plan default), `POST /api/v1/billing/webhook` (Stripe webhook endpoint). All endpoints require auth except webhook. `OnEvent` callback for webhook event logging. 60 new tests (stripe_test.go: client creation/empty key/custom URL, CreateCustomer/GetCustomer/CreateCheckoutSession/trial/GetSubscription/CancelSubscription/CancelImmediately/BillingPortalSession, API error handling, Stripe-Version header, webhook signature valid/missing/invalid format/wrong secret/expired/no tolerance/multiple v1/bad timestamp, PlanPriceMap set/get/PlanForPrice/not found, mapStripeStatus table-driven, StripeError string; webhook_test.go: invalid signature/method not allowed, checkout completed/existing subscription/non-subscription, subscription updated/deleted, payment failed/succeeded recovery/already active/no subscription, unknown event, OnEvent callback; API: checkout unauthorized/invalid plan/free plan/no price/missing URLs/invalid JSON, portal unauthorized/no subscription/missing return URL, get subscription none/with sub/unauthorized, RegisterRoutes). |
| 2026-03-07 | B1 complete: Plan definitions (config-driven). New `pkg/billing` package with config-driven plan catalogue and subscription store. `Plan` model with pricing (monthly/yearly in cents), `PlanLimits` struct covering 13 dimensions (agents, messages/month, tokens/month, integrations, storage, team members, allowed models, custom skills, API access, rate limits). Four default tiers: Free ($0, 1 agent, 500 msg/mo), Starter ($9/mo, 3 agents, 5K msg/mo), Pro ($29/mo, 10 agents, 50K msg/mo), Enterprise (custom, unlimited). `Catalogue` struct with `sync.RWMutex`-guarded concurrent-safe reads + atomic Replace/Merge for hot reloading. `SubscriptionStore` interface + `SQLiteSubscriptionStore` backend with full lifecycle: Create/GetByID/GetByUserID/Update/ListByStatus. `Subscription` model with 6 status states (active/trialing/past_due/canceled/expired/paused), `IsActive()` helper, Stripe customer/subscription ID fields, billing interval, cancel-at-period-end flag. REST API: `GET /api/v1/billing/plans` (list active plans sorted by tier), `GET /api/v1/billing/plans/{id}` (single plan with 404). Limit enforcement helpers: `CheckLimit(resource, usage, limit)` returns descriptive error on overage, `IsUnlimited()`/`IsUnlimitedInt()` for 0-means-unlimited convention. JSON round-trip with `PlanFromJSON`/`PlansFromJSON` for external config loading with plan ID validation. Migration `011_create_subscriptions.sql` with indexes on user_id and status. 50 new tests (plans: ValidPlanID, AllPlanIDs, DefaultPlans completeness/active/models, Catalogue default/not-found/copy-safety/list-sorted/list-active/replace/merge, limits unlimited/check/error-message, JSON from/invalid/plans-map/bad-key/round-trip, sort stability/same-order; subscription store: nil DB, create/duplicate/nil/empty-id/invalid-plan/invalid-status/default-status, get-by-id/not-found, get-by-user/latest/not-found, update/not-found/nil/invalid-status/invalid-plan, list-by-status/empty/invalid, IsActive table, ValidSubscriptionStatus table, persistence reopen, Stripe fields, close; API: list/filter-inactive/get/not-found/free/enterprise/content-type). **Phase 3 started.** |
| 2026-03-07 | U8 complete: Admin API. New `pkg/admin` package with `AdminMiddleware` (role-based access control via `role` column on users table) and comprehensive admin REST API. `AdminMiddleware` checks user role from store — only `admin` role passes, `user` role gets 403 Forbidden, unauthenticated gets 401. Endpoints: `GET /api/v1/admin/users` (list with status/role filters + pagination), `GET /api/v1/admin/users/{id}` (get user details), `PUT /api/v1/admin/users/{id}` (update display name, role, status with partial update support), `DELETE /api/v1/admin/users/{id}` (hard delete), `POST /api/v1/admin/users/{id}/suspend` (set status to suspended), `POST /api/v1/admin/users/{id}/activate` (set status to active), `POST /api/v1/admin/users/{id}/role` (set role with validation), `GET /api/v1/admin/stats` (platform statistics: total/active/pending/suspended user counts), `GET /api/v1/admin/audit` (audit log query with all filters). Safety guards: admins cannot demote themselves, suspend themselves, or delete themselves via admin API. All mutating actions logged to audit store. User model updated with `Role` field (constants `RoleUser`/`RoleAdmin`). Migration `010_add_user_role.sql` adds `role TEXT NOT NULL DEFAULT 'user'` column + index. SQLite store updated: Create/GetByID/GetByEmail/Update/List all include role field. 36 new tests covering: middleware (admin allowed, regular user denied, no auth, user not found), list users (success, status filter, role filter, pagination, empty), get user (success, not found), update user (success, invalid role, self-demotion, self-suspend, not found, invalid JSON), delete user (success, self-deletion, not found), suspend (success, self-suspend, not found), activate (success, not found), set role (success, invalid role, self-demotion, not found, invalid JSON), stats (success with counts), audit events (success, with filter, no store), user role default. **Phase 2 complete.** |
| 2026-03-07 | U7 complete: Audit logging. New `pkg/audit` package with `AuditStore` interface + `SQLiteAuditStore` backend. `Event` model with fluent builder pattern: `NewEvent(action)` → `WithUser/WithActor/WithResource/WithDetail/WithFailure/WithIPAddress/WithUserAgent/WithTimestamp`. 21 action constants covering auth (login, login_failed, logout, register, token_refresh, email_verified, password_changed), agents (created, updated, deleted, default_set), tools (executed, failed), config (updated, plan_changed), admin (user_suspended, user_activated, user_deleted). `QueryFilter` supports user_id, action (exact match + category prefix e.g. "auth."), resource, resource_id, status, time range (since/until), pagination (limit capped at 1000, offset). `Logger` wrapper with configurable `OnError` handler for fire-and-forget logging. HTTP `Middleware` with `WrapHandler(action, resource, handler)` captures response status codes, client IP (X-Forwarded-For → X-Real-IP → RemoteAddr), user agent. API endpoints: `GET /api/v1/audit/events` (filtered list with count/limit/offset response), `GET /api/v1/audit/events/count` (filtered count). `DeleteBefore(timestamp)` for retention policy enforcement. Migration `009_create_audit_log.sql` with indexes on user_id, action, timestamp, (resource, resource_id). 36 new tests covering: event creation/builders/failure/detail JSON/timestamp override, store nil DB/log success/nil event/empty action/detail persistence, query by action/action prefix/resource/resource ID/status/time range/combined filters/pagination/default limit/max limit/empty result/ordering, count, delete before, persistence across store reopen, logger log/error handler/silent error, API list/list filtered/list empty/count/time filter, middleware wrap/failure capture/X-Forwarded-For/X-Real-IP, extractIP remote addr/no port, close. |
| 2026-03-07 | U6 complete: Per-user rate limiting. New `pkg/ratelimit` package with token bucket algorithm. `Limiter` struct provides in-memory per-user rate limiting with configurable plan tiers: free (10 RPM, burst 15, 500/day), starter (30 RPM, burst 50, 5K/day), pro (60 RPM, burst 100, 50K/day), enterprise (120 RPM, burst 200, unlimited). `SQLiteRateLimitStore` backend for state persistence across restarts via `SaveBucket`/`LoadBucket`/`DeleteBucket`. HTTP `Middleware` integrates with `AuthMiddleware` context, adds `X-RateLimit-Limit`, `X-RateLimit-Remaining`, `X-RateLimit-Daily-Limit`, `X-RateLimit-Daily-Remaining`, `X-RateLimit-Reset` headers, returns 429 with `Retry-After` on limit. `StatusHandler` at `GET /api/v1/rate-limit/status` returns full status JSON. `PersistMiddleware` runs periodic background save with graceful shutdown. `AllowAt()` for time-controlled testing. `SetUserPlan` resets bucket on plan change (upgrade/downgrade). `RestoreUser` for loading from store on-demand. `RemoveUser` cleans up memory and store. Migration `008_create_rate_limits.sql`. 34 new tests covering: token bucket exhaustion/refill, daily limits/reset, burst, plan upgrade, multi-user isolation, SQLite CRUD/upsert/delete, persist+restore integration, middleware allow/block/no-auth/no-plan, status handler auth/no-auth/no-plan, persist middleware lifecycle, enterprise unlimited, format helpers. |
| 2026-03-07 | U3 complete: Email verification flow. New `VerificationStore` interface + `SQLiteVerificationStore` backend in `pkg/users/verification.go`. Crypto-random 32-byte hex tokens with configurable TTL (default 24h). `VerifyEmail()` function validates token existence, expiry, and usage status; marks user's email as verified and transitions status from `pending_verification` to `active`. `ResendVerification()` with configurable cooldown enforcement (default 60s) prevents spam. API endpoints: `POST /api/v1/auth/verify-email` with distinct HTTP status codes per error (404 not found, 410 expired, 409 used/already verified), `POST /api/v1/auth/resend-verification` with anti-enumeration (returns 200 OK for nonexistent emails, 409 for already verified, 429 for cooldown). `DeleteExpired()` for periodic token cleanup. `NewAPIFull()` constructor for API with all services. Migration `007_create_verification_tokens.sql` with indexes on user_id and token. 32 new tests covering: store CRUD (create, get, not found, mark used, last token time, delete expired), verification logic (success, not found, expired, used, already verified), resend logic (success, already verified, cooldown, user not found), API handlers (verify success/missing/invalid JSON/not found/expired/used/no store, resend success/missing email/nonexistent/already verified/invalid JSON/no store), persistence across DB reopen, full registration→verification flow. |
| 2026-03-07 | U5 complete: Per-user agent configuration. New `pkg/agents` package with `UserAgentStore` interface + `SQLiteUserAgentStore` backend. Full REST API: `GET/POST /api/v1/agents`, `GET/PUT/DELETE /api/v1/agents/{id}`, `POST /api/v1/agents/{id}/default`. Each agent stores: name (unique per user), description, system_prompt (persona, up to 50K chars), model, model_fallbacks, tools, skills, max_tokens, temperature, max_iterations, is_default, status (active/archived). Per-user isolation enforced at API layer. Max 50 agents per user. Partial update support via PUT (only specified fields change). `SetDefault` uses transaction to atomically clear old + set new default. Migration `006_create_user_agents.sql` with composite unique index on (user_id, name). Exported `ContextKeyUserID()` from `pkg/users` for cross-package context injection. 40 new tests: store (22) — create, duplicate name, same-name-different-users, get by ID, not found, update, update not found, update duplicate name, delete, delete not found, list by user, count by user, get default, get default not found, set default, set default not found, set default wrong user, nil temperature, empty slices, custom ID, persistence, marshal/unmarshal helpers; API (18) — create, missing name, name too long, duplicate, invalid JSON, get, get not found, list, list empty, update, update not found, invalid status, delete, delete not found, set default, set default not found, temperature, prompt too long. |
| 2026-03-07 | U4 complete: Tenant-scoped sessions. New `TenantStore` in `pkg/session/tenant.go` implements `SessionStore` with full tenant-level isolation. All operations scoped via `tenant_id` column and composite key format `tenant:<tenantID>:<originalKey>`. `TenantStoreFactory` creates per-tenant stores from a shared `*sql.DB`. Context propagation helpers `WithTenantID(ctx, id)` / `TenantIDFromContext(ctx)`. Migration `005_add_tenant_id.sql` adds `tenant_id TEXT NOT NULL DEFAULT ''` column + `idx_sessions_tenant` and `idx_sessions_tenant_updated` indexes. Implements `EvictableStore` methods (SessionCount, DeleteSession, EvictExpired, EvictLRU) all tenant-scoped. `ListSessions()` returns unscoped original keys. Updated `initSchema` in `SQLiteStore` to include `tenant_id` column for new databases. 25 new tests covering: context helpers, tenant isolation (cross-tenant read/write/delete blocked), CRUD operations, summary, set/truncate history, session count, eviction (TTL + LRU with cross-tenant safety), list sessions, factory, tool calls round-trip, save/close no-ops, validation. |
| 2026-03-07 | U2 complete: Login + JWT issuance. New `jwt.go` with `TokenService` (HMAC-SHA256 via `golang-jwt/jwt/v5`), `TokenClaims` extending `jwt.RegisteredClaims` with `uid`/`email`/`type` claims, `IssueTokenPair()` for access (15min) + refresh (7d) tokens, `ValidateAccessToken()`/`ValidateRefreshToken()` type-checked validation. New `middleware.go` with `AuthMiddleware` (extracts Bearer token, validates, injects claims into context), `UserIDFromContext()`/`EmailFromContext()`/`ClaimsFromContext()` helpers. Updated `api.go`: `NewAPIWithAuth()` constructor, `POST /api/v1/auth/login` (email lookup, status checks for suspended/deleted, bcrypt verify, token issuance, anti-enumeration errors), `POST /api/v1/auth/refresh` (validates refresh token type, re-checks user existence/status, issues new pair). 28 new tests in `jwt_test.go` (service creation, empty/nil key, options, issuance, access/refresh validation, wrong key, expired, type rejection, unique JTI, middleware valid/missing/invalid/refresh-rejected/malformed, context helpers) and `login_test.go` (success, wrong password, nonexistent user, missing email/password, invalid JSON, case-insensitive, suspended, deleted, no token service, refresh success/invalid/access-rejected/missing/suspended/deleted/invalid-JSON/no-service, full auth flow). |
| 2026-03-06 | U1 complete: Users table + registration API. New `pkg/users` package with `UserStore` interface, `SQLiteUserStore` implementation (CRUD, case-insensitive email, UUID generation), `API` HTTP handler for `POST /api/v1/auth/register` (email validation via net/mail, bcrypt cost-12 hashing, password strength check ≥8 chars). Migration `004_create_users.sql` adds users table with status, email_verified, indexes. 28 new tests (store: create, duplicate, case-insensitive, get by ID/email, update, delete, list, count, persistence, custom ID, shared DB; API: success, duplicate, missing email, invalid email, weak password, invalid JSON, case normalization, empty password, whitespace trimming; password: hash, match, mismatch, validation table-driven). |
| 2026-03-06 | F8 complete: Database migration framework. New `pkg/dbmigrate` package with embedded SQL migrations, `schema_migrations` version tracking table, transactional per-migration execution, idempotent `Up()`, `AutoMigrate()` convenience. 3 built-in migrations consolidating existing schemas (sessions, state, credentials). `Migrator` supports both `embed.FS` and programmatic `NewFromList()`. 17 new tests covering: nil DB, bad dir, duplicates, full apply, idempotency, incremental, applied/pending/version queries, failed migration rollback, non-SQL file filtering, embedded migrations, FK-dependent ordering. **Phase 1 complete.** |
| 2026-03-06 | F7 complete: Automated SQLite backup. New `pkg/backup` package with `VacuumInto()` for atomic snapshots using SQLite's VACUUM INTO. `Scheduler` struct runs periodic backups with configurable interval (default 6h), retention limit (default 7), and automatic pruning of oldest backups. `ListBackups()` lists existing backups sorted chronologically. `Config` struct with `DefaultConfig()`. 14 new tests covering: VacuumInto success/failure, scheduler validation, directory creation, RunOnce, Start/Stop lifecycle, prune logic (over/under limit, non-DB file filtering), list sorting, multiple backups with pruning, backup content verification. |
| 2026-03-06 | F6 complete: Session TTL and eviction. New `EvictableStore` interface with `SessionCount`, `DeleteSession`, `EvictExpired(ttl)`, `EvictLRU(maxSessions)`. SQLiteStore implements all methods (CASCADE deletes for messages). `Evictor` struct runs background goroutine with configurable interval; `RunOnce()` for manual sweeps. `DefaultEvictorConfig()`: 24h TTL, 10K max sessions, 5min sweep. 14 new tests covering: count, delete, TTL eviction, LRU eviction, combined TTL+LRU, no-op cases, start/stop lifecycle, default config. |
| 2026-03-06 | F5 complete: Prometheus metrics endpoint. New `pkg/metrics` package with `prometheus/client_golang`. 11 collectors: LLM (request_duration_seconds histogram, tokens_total counter, errors_total counter), Sessions (active gauge, messages_total counter), Bus (messages_total counter, queue_depth gauge), Tools (execution_duration_seconds histogram, executions_total counter), System (uptime_seconds gauge, info gauge). Instrumented ToolRegistry.ExecuteWithContext and MessageBus.Publish*. Registered `/metrics` on health server. 11 new tests. |
| 2026-03-06 | F4 complete: Structured logging with zerolog. Replaced pkg/logger internals with rs/zerolog while preserving all 20 existing API functions. Added 12 context-aware Ctx functions with correlation ID propagation. JSON/console output modes via OPERATOR_LOG_FORMAT env. Log level via OPERATOR_LOG_LEVEL env. Multi-writer file logging. 10 new tests. |
| 2026-03-06 | F3 complete: Encrypted SQLite credential store with CredentialStore interface, SQLiteCredentialStore implementation, AES-256-GCM + Argon2id encryption. 22 new tests (7 encrypt + 15 store). Package-level functions delegate via SetGlobalCredentialStore(). |
| 2026-03-06 | F2 complete: SQLite state store with StateStore interface, SQLiteStateStore implementation, 9 new tests. Manager delegates to store via NewManagerWithStore(). |
| 2026-03-06 | F1 complete: SQLite session store with SessionStore interface, SQLiteStore implementation, 15 new tests. Fixed pre-existing auth/oauth.go compile error. |
| 2026-03-06 | Initial assessment completed. Branch `operatoros-production-readiness` created. STATUS.md and CLAUDE.md written. |
